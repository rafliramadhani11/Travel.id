$(window).on('load', function () {
  $('.hero .text1').css({
    opacity: '1',
    transform: 'translateX(0)',
  });

  $('.hero .img-hero').css({
    opacity: '1',
    transform: 'translateX(0)',
  });
});
